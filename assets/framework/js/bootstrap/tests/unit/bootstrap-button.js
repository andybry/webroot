$(function(){module("bootstrap-buttons");test("should be defined on jquery object",function(){ok($(document.body).button,"button method is defined")});test("should return element",function(){ok($(document.body).button()[0]==document.body,"document.body returned")});test("should return set state to loading",function(){var a=$('<button class="btn" data-loading-text="fat">mdo</button>');equals(a.html(),"mdo","btn text equals mdo");a.button("loading");equals(a.html(),"fat","btn text equals fat");stop();setTimeout(function(){ok(a.attr("disabled"),"btn is disabled");ok(a.hasClass("disabled"),"btn has disabled class");start()},0)});test("should return reset state",function(){var a=$('<button class="btn" data-loading-text="fat">mdo</button>');equals(a.html(),"mdo","btn text equals mdo");a.button("loading");equals(a.html(),"fat","btn text equals fat");stop();setTimeout(function(){ok(a.attr("disabled"),"btn is disabled");ok(a.hasClass("disabled"),"btn has disabled class");start();stop()},0);a.button("reset");equals(a.html(),"mdo","btn text equals mdo");setTimeout(function(){ok(!a.attr("disabled"),"btn is not disabled");ok(!a.hasClass("disabled"),"btn does not have disabled class");start()},0)});test("should toggle active",function(){var a=$('<button class="btn">mdo</button>');ok(!a.hasClass("active"),"btn does not have active class");a.button("toggle");ok(a.hasClass("active"),"btn has class active")});test("should toggle active when btn children are clicked",function(){var b=$('<button class="btn" data-toggle="button">mdo</button>'),a=$("<i></i>");b.append(a).appendTo($("#qunit-fixture"));ok(!b.hasClass("active"),"btn does not have active class");a.click();ok(b.hasClass("active"),"btn has class active")});test("should toggle active when btn children are clicked within btn-group",function(){var b=$('<div class="btn-group" data-toggle="buttons-checkbox"></div>'),c=$('<button class="btn">fat</button>'),a=$("<i></i>");b.append(c.append(a)).appendTo($("#qunit-fixture"));ok(!c.hasClass("active"),"btn does not have active class");a.click();ok(c.hasClass("active"),"btn has class active")})});